<view class="results-container">
  <view class="result-item" wx:for="{{results}}" wx:key="currency">
    <text class="currency">{{item.currency}}</text>
    <text class="value">{{item.value}}</text>
  </view>
</view> 